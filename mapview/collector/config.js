const config = {
  CASSANDRA_ENDPOINT: "localhost",
  CASSANDRA_USER: "mapview_collector",
  CASSANDRA_PASSWORD: "changeme",
  LISTEN_ADDRESS: "localhost",
  LISTEN_PORT: 3000,
}

module.exports = config;
